# Release results

`main_results.json` is the compact, publication-safe result record. It contains no raw evidence, API response, human annotation, or model prediction. Values are copied from frozen local summaries and the synchronized server logs available on 2026-08-10.

Before camera-ready release, replace the pending data-matched source-only entry after the server output is synchronized and independently checked.
